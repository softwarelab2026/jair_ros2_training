services:
  turtlesim:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    container_name: turtlesim_tracker
    environment:
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
